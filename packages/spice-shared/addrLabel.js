/**
 * Address-display helpers — pure functions, no IO.
 * Re-exported from colony-app for backwards compatibility while migrating.
 */
export { shortAddr, namedAddr } from '../../colony-app/src/utils/addrLabel.js'
