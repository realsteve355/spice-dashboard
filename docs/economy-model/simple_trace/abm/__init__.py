"""Mesa-based agent simulation of the AXION toy colony.

This is the scaffolding parallel to the deterministic `simple_trace` toy.
First milestone: reproduce /ledger numbers exactly with 4 Citizen agents and
2 Business agents. If Mesa's output matches the JS-side deterministic
trajectory month-for-month, the plumbing is right and we can scale up.
"""
